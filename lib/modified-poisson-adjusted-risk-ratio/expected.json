{
  "method": "modified-poisson-adjusted-risk-ratio",
  "fixture": "fixture.csv",
  "_why_two_claims": "AGREEMENT asks whether the engines compute the same thing; RECOVERY asks whether they compute the right thing. Agreement alone passes identical mistakes. Recovery alone passes a default mismatch smaller than sampling error.",
  "_why_not_self_generated": "Expected values are not recorded from running this code. The fixture draws the outcome from a LOG-LINK risk model, which is exactly the parameter modified Poisson targets, so the truth is a property of the data rather than of any implementation.",
  "truth": {
    "source": "the fixture's data-generating process",
    "exposure_log_rr": 0.4054651081081644,
    "covariate_beta": 0.2
  },
  "agreement": {
    "claim": "every executed engine reproduces the same estimates AND the same HC0 robust standard error",
    "tolerance_estimate": 0.0001,
    "tolerance_se": 0.0001,
    "_note": "1e-4, matching the other entry, calibrated against the optimiser noise measured on run 3. The SE tolerance is the point of this entry: sandwich::vcovHC defaults to HC3 and statsmodels GLM defaults to the nonrobust covariance, and an HC0-vs-HC3 difference is ~4e-3, about 40x this tolerance. It stays comfortably catchable."
  },
  "recovery": {
    "claim": "the estimate is close to the risk ratio the fixture used",
    "tolerance_estimate": 0.3,
    "_note": "MEASURED. Observed SE 0.0969 (run 3, Python), against the 0.04 guessed before anything ran, so the old 0.12 was 1.2 SE rather than the 3 intended. 0.30 is three observed SEs. The observed miss was 0.0017, or 0.018 SE."
  },
  "must_appear": {
    "r": [
      "glm(",
      "poisson(link",
      "vcovHC"
    ],
    "python": [
      "Poisson",
      "cov_type"
    ],
    "sas": [
      "proc genmod",
      "dist=poisson",
      "repeated"
    ],
    "stata": [
      "glm",
      "family(poisson)"
    ]
  },
  "engines": {
    "r": {
      "executed": true
    },
    "python": {
      "executed": true
    },
    "sas": {
      "executed": false,
      "reason": "no SAS engine licensed"
    },
    "stata": {
      "executed": false,
      "reason": "no Stata licence; and vce(robust) applies a finite-sample correction, so its SE would need its own tolerance rather than HC0's"
    }
  },
  "derived_truth": {
    "exposure_rr": 1.5,
    "note": "Exponentiated generating coefficient for interpretation only; recovery is checked on the log scale in truth."
  }
}
